# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Node (if any tooling added later — no trailing slash to also match symlinks in worktrees)
node_modules

# Test artifacts
*.log
tmp/
lib/test-fixtures-temp/

# Local development state (no trailing slash — matches both dirs and symlinks in worktrees)
.rite
.claude
backend/node_modules
.venv
backend/.venv
